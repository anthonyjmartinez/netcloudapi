# Should accept an object of one of the endpoint classes
# and based on the contained object attributes build the
# appropriate request URL, and call the appropriate requests
# method based on <object>.method (for get, post, ...)
# the response should be the returned status code and JSON object
